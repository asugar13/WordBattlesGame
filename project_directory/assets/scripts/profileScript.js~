// Start the app.
$(document).ready(function() {
	$.get('/user_info', function(data){
		var answer = data.answer;
		if (answer == "OK") {
			let parent = $('#info');
		    let p = $('<p>').text("Username: " + data.username);
		    p.attr('id', 'username');
		    parent.append(p);

		    p = $('<p>').text("Score: " + data.score);
		    p.attr('id', 'score');
		    parent.append(p);
		}
	});
	/*$('#userSearch').click(function(evt) {
		var formData = $('#lookupForm').serialize();
		console.log("script: " + formData);
		$.get('/user_lookup', formData, function(data) {
			console.log(data);
			var answer = data.answer;
			if (answer == "OK") {
				let parent = $('#lookupInfo');
			    let p = $('<p>').text("Username: " + data.username);
			    p.attr('id', 'lookup_username');
			    parent.append(p);

			    p = $('<p>').text("Score: " + data.score);
			    p.attr('id', 'lookup_score');
			    parent.append(p);
			}
		});	
	});*/
	
	
});
